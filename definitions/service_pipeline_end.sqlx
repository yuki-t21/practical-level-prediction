config {
  type: "operations",
  name: "service_pipeline_end",
  tags: ["service_features_pipeline"],
  dependencies: ["explain_predictions_operation"],
}

SELECT `${dataform.projectConfig.defaultDatabase}.ml_models.send_slack`(
  '#predictions-alert',
  CONCAT('✅ サービス固有パイプライン (service_features_pipeline) が完了しました。(対象サービスID: ', dataform.projectConfig.vars.service_id, ')')
)
